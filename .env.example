# Optional overrides. The app ships with working defaults baked in
# (the Supabase publishable key is safe to expose in a public client),
# so you normally don't need this file at all.
VITE_SUPABASE_URL=https://jhgovaukkxdgoqqhqufs.supabase.co
VITE_SUPABASE_KEY=sb_publishable_dGUaEMxjZ8c_pvxE6Y6KYQ_F794ChSt
# Set to "/" if deploying to a custom domain or Vercel instead of GitHub Pages.
# VITE_BASE=/pirateproject/
